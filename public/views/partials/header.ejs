<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - StockFake</title>
<%- include('styles') %>
<% if (typeof pageStyles !== 'undefined') { %>
    <style>
        <%# Note: pageStyles is server-controlled content from EJS templates, not user input %>
        <%- pageStyles %>
    </style>
<% } %>
</head>
<body>
    <div class="container">
        <h1><%= pageTitle %></h1>
<%- include('navigation') %>
